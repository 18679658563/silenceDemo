spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/18679658563/silenceDemo.git
          searchPaths: config/src/main/resources/properties
#spring.cloud.config.server.git.username=
#spring.cloud.config.server.git.password=
server:
  port: 9200

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/

